# 阶段三过关题集：级数、微分方程与证明题

> 配套 `阶段三_级数与微分方程与证明技巧.md` 讲义使用
> **用法建议**：先闭卷做完题目篇（建议 90 分钟），再对照答案篇订正。压轴题在本章，B 组务必吃透。

## 建议用时分配

| 篇目 | 题量 | 建议用时 | 说明 |
|---|---|---|---|
| 一、级数（S） | 12 | 30 min | 判敛 + 幂级数收敛域 + 求和/展开 |
| 二、微分方程（E） | 13 | 25 min | 认类型 → 套方法，速度是关键 |
| 三、证明题（P） | 7 | 35 min | 三大武器：辅助函数 / Cauchy / 化二重积分 |
| **合计** | **32** | **90 min** | |

> **本篇是压轴题的所在地**（三届真题第 6 题都出在这里），P 组写不出辅助函数不要慌，先看答案记住套路。

## 讲义对照表

| 题组 | 对应讲义小节 | 要先掌握的结论 |
|---|---|---|
| S（级数） | §3.1 级数 | $p$-级数、比值/根值判别法、幂级数收敛域的**三步写法** |
| E（微分方程） | §3.2 微分方程 | 五大类型识别 + 二阶常系数三种特征根 |
| P（证明题） | §3.3 证明题技巧 | **三大武器**的框架 |

## 难度与题型标记

| 标记 | 含义 |
|---|---|
| ★☆☆ / ★★☆ / ★★★ | 难度：基础过关 / 进阶 / 挑战 |
| `[填空]` | 只写结果，练速度 |
| `[计算]` | 要写关键步骤 |
| `[证明]` | 中值定理、不等式类 |

**技巧标签**：`比较判别法` `Leibniz` `比值判别法` `收敛半径` `端点判定` `几何级数放缩` `裂项` `幂级数求和` `可分离` `齐次` `一阶线性` `Bernoulli` `可降阶` `二阶常系数` `待定系数` `辅助函数` `Cauchy积分不等式` `区间反转` `化二重积分`

---

# 题目篇

## 一、级数（S）

### A 组 · 基础过关

**S-A1** ★☆☆ `[计算]` 判断级数 $\displaystyle\sum_{n=1}^\infty\frac{1}{n^2+n}$ 的收敛性；若收敛，求和。`裂项`

**S-A2** ★☆☆ `[填空]` 判断级数 $\displaystyle\sum_{n=1}^\infty\frac{(-1)^{n-1}}{\sqrt n}$ 的收敛性（绝对还是条件？）。`Leibniz`

**S-A3** ★☆☆ `[填空]` 判断级数 $\displaystyle\sum_{n=1}^\infty\frac{1}{n\cdot2^n}$ 的收敛性。`比较判别法`

**S-A4** ★☆☆ `[填空]` 求幂级数 $\displaystyle\sum_{n=1}^\infty\frac{x^n}{n}$ 的收敛域。`收敛半径 + 端点`

### B 组 · 进阶

**S-B1** ★★☆ `[计算]` 求幂级数 $\displaystyle\sum_{n=1}^\infty\frac{x^n}{n\cdot2^n}$ 的收敛域。`收敛半径 + 端点`

**S-B2** ★★☆ `[计算]` 求幂级数 $\displaystyle\sum_{n=1}^\infty(-1)^{n-1}\frac{x^{2n}}{n(2n-1)}$ 的收敛域。`缺项级数`

**S-B3** ★★☆ `[填空]` 求 $\displaystyle\sum_{n=0}^\infty(n+1)x^n$（$|x|<1$）的和函数。`幂级数求和`

**S-B4** ★★☆ `[计算]` 把 $f(x)=\ln(1+x)$ 展开成幂级数，并求 $\displaystyle\sum_{n=1}^\infty\frac{(-1)^{n-1}}{n}$。`标准展开`

**S-B5** ★★☆ `[计算]` 判断级数 $\displaystyle\sum_{n=1}^\infty\frac{n}{2^n}$ 的收敛性；若收敛，求和。`比值 + 求和`

### C 组 · 挑战

**S-C1** ★★★ `[计算]` 判断级数 $\displaystyle\sum_{n=1}^\infty\sin\frac{1}{n^2}$ 的收敛性。`等价无穷小比较`

**S-C2** ★★★ `[证明]` 设级数 $\displaystyle\sum_{n=1}^\infty a_n$ 绝对收敛。证明 $\displaystyle\sum_{n=1}^\infty a_n^2$ 收敛。`绝对收敛的性质`

**S-C3** ★★★ `[计算]` 求幂级数 $\displaystyle\sum_{n=1}^\infty\frac{x^n}{n^2}$ 的收敛域，并求 $\displaystyle\sum_{n=1}^\infty\frac{1}{n^2}$。`端点 + 已知和`

## 二、微分方程（E）

### A 组 · 基础过关

**E-A1** ★☆☆ `[计算]` 求 $y'=2xy$ 的通解。

**E-A2** ★☆☆ `[计算]` 求 $y'=\dfrac{y}{x}+1$ 的通解。

**E-A3** ★☆☆ `[计算]` 求 $y'-\dfrac yx=x$ 的通解。

**E-A4** ★☆☆ `[计算]` 求 $y''-3y'+2y=0$ 的通解。

### B 组 · 进阶

**E-B1** ★★☆ `[计算]` 求 $y''+4y=0$ 的通解。

**E-B2** ★★☆ `[计算]` 求 $y''-2y'+y=0$ 的通解。

**E-B3** ★★☆ `[计算]` 求 $y'+y=e^x$ 的通解。

**E-B4** ★★☆ `[计算]` 求 $y'=x+y$ 的通解（一阶线性）。

**E-B5** ★★☆ `[计算]` 求 $y'=\dfrac{y}{x}+\dfrac{x}{y}$ 的通解（$x>0$）。`Bernoulli`

**E-B6** ★★☆ `[计算]` 求 $y''=2x$ 的通解（可降阶）。

### C 组 · 挑战

**E-C1** ★★★ `[计算]` 求 $y''-5y'+6y=e^x$ 的通解。`待定系数`

**E-C2** ★★★ `[计算]` 求 $y\cdot y''=(y')^2$ 的通解（缺 $x$）。`可降阶`

**E-C3** ★★★ `[计算]` 求 $y'+y\tan x=\sec x$ 的通解（$-\frac\pi2<x<\frac\pi2$）。`一阶线性`

## 三、证明题（P）

**P-A1** ★☆☆ `[证明]` 设 $f$ 在 $[0,1]$ 连续、$(0,1)$ 可导，$f(0)=0,\ f(1)=1$。证明：存在 $\xi\in(0,1)$ 使 $f'(\xi)=2\xi$。`辅助函数`

**P-A2** ★☆☆ `[证明]` 设 $f$ 在 $[a,b]$ 连续、$(a,b)$ 可导且 $a>0$。证明：存在 $\xi\in(a,b)$ 使
$$f(b)-f(a)=\frac{b^2-a^2}{2\xi}f'(\xi).\qquad\text{柯西}$$

**P-B1** ★★☆ `[证明]` 设 $f$ 在 $[a,b]$ 连续、$(a,b)$ 可导 $(0<a<b)$。证明：存在 $\xi\in(a,b)$ 使
$$\frac{bf(b)-af(a)}{b-a}=f(\xi)+\xi f'(\xi).\qquad\text{乘 }xf(x)\text{'}$$

**P-B2** ★★☆ `[证明]` 设 $f$ 在 $[0,1]$ 上有连续导数，$f(0)=0$。证明
$$\int_0^1f^2(x)\,dx\le\frac12\int_0^1\big(f'(x)\big)^2dx.\qquad\text{Cauchy 积分不等式}$$

**P-B3** ★★☆ `[证明]` 设 $f$ 在 $[0,1]$ 上连续且单调递增。证明
$$\int_0^1xf(x)\,dx\ \ge\ \frac12\int_0^1f(x)\,dx.\qquad\text{减去常数}$$

**P-C1** ★★★ `[证明]` 设 $f$ 在 $[0,1]$ 连续、$(0,1)$ 可导，$f(0)=0,\ f(1)=1$。证明：存在**两个不同**的 $\xi,\eta\in(0,1)$ 使
$$\frac1{f'(\xi)}+\frac1{f'(\eta)}=2.\qquad\text{介值 + 两次拉氏}$$

**P-C2** ★★★ `[证明]` 设 $f$ 在 $[0,1]$ 上有连续二阶导数，$f(0)=f(1)=0$，且 $\big|f''(x)\big|\le M$。证明
$$\left|\int_0^1f(x)\,dx\right|\le\frac{M}{12}.$$

---
---

# 答案篇

> 每题给出 **思路 → 关键步骤 → 答案 → ⚠ 易错点**。

## 一、级数

### S-A1 ★☆☆ 答案 收敛，和为 $1$

**裂项**：$\dfrac{1}{n^2+n}=\dfrac{1}{n(n+1)}=\dfrac1n-\dfrac{1}{n+1}$。

部分和 $S_N=\displaystyle\sum_{n=1}^N\Big(\frac1n-\frac1{n+1}\Big)=1-\frac{1}{N+1}\to1$。

故级数收敛，$\displaystyle\sum_{n=1}^\infty\frac{1}{n^2+n}=1$。

⚠ 这是**裂项相消**的标准情形。看到 $\frac{1}{n(n+k)}$ 就拆成 $\frac1k\big(\frac1n-\frac1{n+k}\big)$。别用比较判别法（能判敛但求不出和）。

### S-A2 ★☆☆ 答案 条件收敛（不绝对收敛）

$a_n=\frac{1}{\sqrt n}$ 单调递减且趋于 0，由 **Leibniz 判别法**交错级数收敛。

但 $\displaystyle\sum\frac{1}{\sqrt n}=\sum\frac{1}{n^{1/2}}$ 是 $p=\frac12\le1$ 的 $p$-级数，**发散**。

故原级数**条件收敛**。

⚠ **必须区分"收敛"与"绝对收敛"**：题目问"绝对还是条件"时两层都要算。$p$-级数 $\sum\frac1{n^p}$ 在 $p\le1$ 时发散——**$p=1$ 时（调和级数）也发散**，这是最常见的记错点。

### S-A3 ★☆☆ 答案 收敛

$\dfrac{1}{n\cdot2^n}\le\dfrac{1}{2^n}$，而 $\sum\frac{1}{2^n}$ 是收敛的几何级数（公比 $\frac12<1$），故由比较判别法收敛。

（更精确：$\sum_{n=1}^\infty\frac{1}{n2^n}=\ln2$。）

⚠ 放缩时找"更大但收敛"的级数。**几何级数是最常用的比较对象**。

### S-A4 ★☆☆ 答案 $[-1,\ 1)$

系数 $a_n=\frac1n$：$R=\lim\big|\frac{a_n}{a_{n+1}}\big|=\lim\frac{n+1}{n}=1$。

**端点**：
- $x=1$：$\sum\frac1n$ 是调和级数，**发散**
- $x=-1$：$\sum\frac{(-1)^n}{n}$ 由 Leibniz 判别法**收敛**

故收敛域为 $[-1,1)$。

⚠ **收敛域三步写法**：① 算 $R$；② $x=R$ 处判；③ $x=-R$ 处判。**两个端点结论可能不同**，必须分开写，不能只写"$[-1,1]$"或"$(-1,1)$"。

### S-B1 ★★☆ 答案 $[-2,\ 2)$

$a_n=\frac{1}{n2^n}$，$R=\lim\Big|\frac{a_n}{a_{n+1}}\Big|=\lim\frac{(n+1)2^{n+1}}{n2^n}=2$。

**端点**：
- $x=2$：$\sum\frac{2^n}{n2^n}=\sum\frac1n$ **发散**
- $x=-2$：$\sum\frac{(-2)^n}{n2^n}=\sum\frac{(-1)^n}{n}$ **收敛**（Leibniz）

故收敛域 $[-2,2)$。

⚠ 与 S-A4 同型，只是半径从 1 变 2。**端点判定和 $R$ 无关，永远要单独做**。

### S-B2 ★★☆ 答案 $[-1,\ 1]$

这是**缺项级数**（只有偶次幂），令 $t=x^2$，先看 $\sum(-1)^{n-1}\frac{t^n}{n(2n-1)}$。

$$R_t=\lim\Big|\frac{a_n}{a_{n+1}}\Big|=\lim\frac{(n+1)(2n+1)}{n(2n-1)}=1$$
故 $|t|<1$ 收敛，即 $|x|<1$ 收敛。

**端点**：$t=1$ 即 $x=\pm1$ 时，$\sum\frac{(-1)^{n-1}}{n(2n-1)}$ 是交错级数且 $\frac{1}{n(2n-1)}$ 单调递减趋于 0，**收敛**。

故收敛域为 $[-1,1]$。

⚠ ① 缺项级数**把 $x^2$ 当整体**（或令 $t=x^2$），不要硬套 $R$ 公式；② $x=\pm1$ 代入后**级数完全相同**（因为只有偶次幂），但**两步都要写上**，别只判一个。

### S-B3 ★★☆ 答案 $\dfrac{1}{(1-x)^2}$

由几何级数 $\displaystyle\sum_{n=0}^\infty x^n=\frac{1}{1-x}$（$|x|<1$），两边求导：
$$\sum_{n=1}^\infty nx^{n-1}=\frac{1}{(1-x)^2}$$
故 $\displaystyle\sum_{n=0}^\infty(n+1)x^n=\sum_{n=1}^\infty nx^{n-1}=\frac{1}{(1-x)^2}$。

⚠ **幂级数求和的两大手段：逐项求导、逐项积分**。看到系数是 $n+1$、$n$ 就往"几何级数求导"上想；看到分母是 $n$、$n+1$ 就往"几何级数积分"上想。

### S-B4 ★★☆ 答案 $\ln(1+x)=\displaystyle\sum_{n=1}^\infty\frac{(-1)^{n-1}}{n}x^n\ (-1<x\le1)$；$\displaystyle\sum_{n=1}^\infty\frac{(-1)^{n-1}}{n}=\ln2$

由 $\dfrac{1}{1+t}=\sum_{n=0}^\infty(-1)^nt^n$ 在 $[0,x]$ 上逐项积分：
$$\ln(1+x)=\int_0^x\frac{dt}{1+t}=\sum_{n=0}^\infty(-1)^n\frac{x^{n+1}}{n+1}=\sum_{n=1}^\infty\frac{(-1)^{n-1}}{n}x^n$$

取 $x=1$（级数在 $x=1$ 收敛，由 Abel 定理可代）：
$$\sum_{n=1}^\infty\frac{(-1)^{n-1}}{n}=1-\frac12+\frac13-\frac14+\cdots=\ln2$$

⚠ **收敛域是 $-1<x\le1$（左开右闭）**：$x=-1$ 时是调和级数发散，$x=1$ 时收敛。这个"半开半闭"最容易写错。

### S-B5 ★★☆ 答案 收敛，和为 $2$

$a_n=\frac{n}{2^n}$，$R=\lim\frac{a_{n+1}}{a_n}$ 的倒数 $=\lim\frac{(n+1)/2^{n+1}}{n/2^n}=\frac12<1$，故收敛。

**求和**：由 $\sum_{n=1}^\infty nx^{n-1}=\frac{1}{(1-x)^2}$，取 $x=\frac12$：
$$\sum_{n=1}^\infty\frac{n}{2^n}=\frac12\sum_{n=1}^\infty n\Big(\frac12\Big)^{n-1}=\frac12\cdot\frac{1}{(1-\frac12)^2}=\frac12\cdot4=2$$

⚠ 求和时要**把 $x^{n-1}$ 的结构凑出来**：$\frac{n}{2^n}=x\cdot nx^{n-1}\big|_{x=1/2}$，别直接用 $\sum nx^n$ 的公式（那是 $\frac{x}{(1-x)^2}$，也行但多一步）。

### S-C1 ★★★ 答案 收敛

当 $n\to\infty$ 时 $\sin\frac{1}{n^2}\sim\frac{1}{n^2}$。而 $\sum\frac{1}{n^2}$ 是 $p=2>1$ 的 $p$-级数，**收敛**。

由比较判别法的极限形式，原级数**收敛**。

⚠ 含三角函数的级数：**先用等价无穷小换成幂函数，再和 $p$-级数比**。注意 $n\to\infty$ 时 $\frac{1}{n^2}\to0$，故 $\sin\frac{1}{n^2}\sim\frac{1}{n^2}$ 成立。

### S-C2 ★★★ 提示

$\sum a_n$ 绝对收敛 ⟹ $\sum|a_n|$ 收敛 ⟹ $|a_n|\to0$。故存在 $N$，当 $n>N$ 时 $|a_n|\le1$。

于是 $a_n^2=|a_n|^2\le|a_n|$（$n>N$），而 $\sum|a_n|$ 收敛，由比较判别法 $\sum a_n^2$ 收敛。∎

⚠ 关键一步是**用 $|a_n|\to0$ 得到 $|a_n|\le1$**（从某项开始）。这是"绝对收敛 ⟹ 平方收敛"的标准证法。

### S-C3 ★★★ 答案 收敛域 $[-1,1]$；$\displaystyle\sum_{n=1}^\infty\frac{1}{n^2}=\frac{\pi^2}{6}$

$a_n=\frac{1}{n^2}$，$R=\lim\frac{(n+1)^2}{n^2}=1$。

**端点**：$x=1$ 时 $\sum\frac{1}{n^2}$ 是 $p=2$ 的 $p$-级数**收敛**；$x=-1$ 时 $\sum\frac{(-1)^n}{n^2}$ **绝对收敛**。故收敛域 $[-1,1]$。

$x=1$ 处的和是经典结果：$\displaystyle\sum_{n=1}^\infty\frac{1}{n^2}=\frac{\pi^2}{6}$。

⚠ 这个和（Euler 结果）在初赛里**当已知结论直接用**即可。若题目问"求 $\sum\frac{1}{n^2}$"，答 $\frac{\pi^2}{6}$。

## 二、微分方程

### E-A1 ★☆☆ 答案 $y=Ce^{x^2}$

**可分离变量**：$\dfrac{dy}{y}=2x\,dx$，积分 $\ln|y|=x^2+C_1$，故 $y=Ce^{x^2}$。

⚠ 分离变量时**两边都要积分**，且 $\int\frac{dy}{y}=\ln|y|$ 有绝对值。若 $y\equiv0$ 也是解（对应 $C=0$），已包含在通解里。

### E-A2 ★☆☆ 答案 $y=x(\ln x+C)$

**齐次方程**：$\dfrac{dy}{dx}=\dfrac yx+1$。令 $u=\dfrac yx$，则 $y=ux,\ y'=u+xu'$：
$$u+xu'=u+1\ \Longrightarrow\ xu'=1\ \Longrightarrow\ du=\frac{dx}{x}$$
$$u=\ln|x|+C\ \Longrightarrow\ \boxed{y=x(\ln x+C)}$$

⚠ **齐次方程的识别**：右端只含 $\frac yx$ 的组合。换元 $u=\frac yx$ 是固定动作。

### E-A3 ★☆☆ 答案 $y=x^2+Cx$

**一阶线性**：$y'+P(x)y=Q(x)$，此处 $P=-\frac1x,\ Q=x$。
$$\int P\,dx=-\ln x,\qquad e^{\int Pdx}=\frac1x,\qquad e^{-\int Pdx}=x$$
$$y=e^{-\int Pdx}\Big[\int Qe^{\int Pdx}dx+C\Big]=x\Big[\int x\cdot\frac1x\,dx+C\Big]=x(x+C)=\boxed{x^2+Cx}$$

⚠ **必须先化成标准形式 $y'+Py=Q$** 再套公式。本题已是标准形式；若给的是 $xy'-y=x^2$，要先除以 $x$。

### E-A4 ★☆☆ 答案 $y=C_1e^x+C_2e^{2x}$

特征方程 $r^2-3r+2=0$，$(r-1)(r-2)=0$，$r_1=1,\ r_2=2$ 是**两个不等实根**。

通解 $y=C_1e^{x}+C_2e^{2x}$。

⚠ 二阶常系数齐次的三种情形要背熟：

| 判别式 | 根 | 通解 |
|---|---|---|
| $p^2-4q>0$ | $r_1\ne r_2$ | $C_1e^{r_1x}+C_2e^{r_2x}$ |
| $p^2-4q=0$ | $r$ 二重 | $(C_1+C_2x)e^{rx}$ |
| $p^2-4q<0$ | $\alpha\pm\beta i$ | $e^{\alpha x}(C_1\cos\beta x+C_2\sin\beta x)$ |

### E-B1 ★★☆ 答案 $y=C_1\cos2x+C_2\sin2x$

特征方程 $r^2+4=0$，$r=\pm2i$，即 $\alpha=0,\ \beta=2$，故 $y=C_1\cos2x+C_2\sin2x$。

⚠ 纯虚根时**没有 $e^{\alpha x}$ 因子**（因为 $\alpha=0$），别多写 $e^{0\cdot x}$ 也别忘了写成 $\cos2x$（不是 $\cos x$）。

### E-B2 ★★☆ 答案 $y=(C_1+C_2x)e^{x}$

特征方程 $r^2-2r+1=0$，$(r-1)^2=0$，$r=1$ 是**二重根**，故 $y=(C_1+C_2x)e^{x}$。

⚠ **重根必须补一个 $x$**：写成 $C_1e^x+C_2e^x$ 是错的（那不是通解，只有一个独立常数）。

### E-B3 ★★☆ 答案 $y=Ce^{-x}+\dfrac12e^x$

**一阶线性**：$P=1,\ Q=e^x$。
$$\int Pdx=x,\quad e^{\int P}=e^x,\quad e^{-\int P}=e^{-x}$$
$$y=e^{-x}\Big[\int e^x\cdot e^xdx+C\Big]=e^{-x}\Big[\frac{e^{2x}}{2}+C\Big]=\boxed{\frac{e^x}{2}+Ce^{-x}}$$

⚠ 也可以看成"齐次通解 + 一个特解"：$Ce^{-x}$ 是 $y'+y=0$ 的通解，$\frac{e^x}{2}$ 是特解。

### E-B4 ★★☆ 答案 $y=Ce^{x}-x-1$

**一阶线性**：$y'-y=x$，$P=-1,\ Q=x$。
$$\int Pdx=-x,\quad e^{\int P}=e^{-x},\quad e^{-\int P}=e^{x}$$
$$y=e^{x}\Big[\int x e^{-x}dx+C\Big]=e^{x}\Big[-(x+1)e^{-x}+C\Big]=Ce^{x}-x-1$$

（其中 $\int xe^{-x}dx=-(x+1)e^{-x}$ 用分部积分。）

⚠ $\int xe^{-x}dx$ 的**符号最容易错**，建议分部后立刻代入验算：$(Ce^x-x-1)'=Ce^x-1$，而 $y'-y=(Ce^x-1)-(Ce^x-x-1)=x$ ✓

### E-B5 ★★☆ 答案 $y^2=x^2(2\ln x+C)$

两边乘 $y$：$yy'=\dfrac{y^2}{x}+x$。**令 $u=y^2$**，则 $u'=2yy'$，得
$$\frac{u'}{2}=\frac ux+x\ \Longrightarrow\ u'-\frac2xu=2x\quad(\text{一阶线性})$$
$P=-\frac2x,\ \int Pdx=-2\ln x$，故 $e^{\int P}=x^{-2}$，$e^{-\int P}=x^2$：
$$u=x^2\Big[\int 2x\cdot x^{-2}dx+C\Big]=x^2\big[2\ln x+C\big]$$
$$\boxed{y^2=x^2(2\ln x+C)}\quad\text{即}\ y=\pm x\sqrt{2\ln x+C}$$

⚠ 这是 **Bernoulli 方程**（$y'=\frac yx+\frac xy$ 即 $y'-\frac1xy=xy^{-1}$，$n=-1$）：令 $z=y^{1-n}=y^2$ 化为线性。

### E-B6 ★★☆ 答案 $y=\dfrac{x^3}{3}+C_1x+C_2$

**可降阶（缺 $y$）**：$y''=2x$ 直接积分两次：
$$y'=x^2+C_1,\qquad y=\frac{x^3}{3}+C_1x+C_2$$

⚠ 积分两次就有**两个独立常数**。缺 $y$ 的方程令 $p=y'$ 则 $y''=p'$；缺 $x$ 的方程令 $p=y'$ 则 $y''=p\frac{dp}{dy}$ —— **两者别搞混**（见 E-C2）。

### E-C1 ★★★ 答案 $y=C_1e^{2x}+C_2e^{3x}+\dfrac12e^x$

**第一步：齐次通解。** 特征方程 $r^2-5r+6=0$，$(r-2)(r-3)=0$，故 $y_h=C_1e^{2x}+C_2e^{3x}$。

**第二步：特解（待定系数）。** 右端 $e^x$，且 $\lambda=1$ **不是**特征根，故设 $y^*=Ae^x$：
$$y^{*''}-5y^{*'}+6y^*=Ae^x-5Ae^x+6Ae^x=2Ae^x=e^x\ \Longrightarrow\ A=\frac12$$

**第三步：通解** $y=y_h+y^*=C_1e^{2x}+C_2e^{3x}+\frac12e^x$。

⚠ ① 待定系数前**必须检查 $\lambda$ 是不是特征根**：若不是，设 $Ae^{\lambda x}$；若是一重根，设 $Axe^{\lambda x}$；若是二重根，设 $Ax^2e^{\lambda x}$。② 本题 $\lambda=1$ 不是根（根是 2、3），故设 $Ae^x$。

### E-C2 ★★★ 答案 $y=C_2e^{C_1x}$（即 $y=C_2 e^{C_1 x}$）

**缺 $x$ 型**：令 $p=y'$，则 $y''=p\dfrac{dp}{dy}$，原方程
$$y\cdot p\frac{dp}{dy}=p^2\ \Longrightarrow\ y\frac{dp}{dy}=p\ (\text{设 }p\ne0)\ \Longrightarrow\ \frac{dp}{p}=\frac{dy}{y}$$
$$\ln|p|=\ln|y|+C_1'\ \Longrightarrow\ p=C_1y\ \Longrightarrow\ \frac{dy}{dx}=C_1y\ \Longrightarrow\ y=C_2e^{C_1x}$$

⚠ ① **缺 $x$ 型（方程里不显含 $x$）才用 $y''=p\frac{dp}{dy}$**；若方程含 $x$ 而缺 $y$，则用 $y''=p'$（此时 $p$ 是 $x$ 的函数）。② 令 $p$ 时要注意 $p=0$ 的特例（对应 $y\equiv$ 常数）。

### E-C3 ★★★ 答案 $y=\sin x+C\cos x$（$-\frac\pi2<x<\frac\pi2$）

**一阶线性**：$P(x)=\tan x,\ Q(x)=\sec x$。
$$\int P\,dx=\int\tan x\,dx=-\ln|\cos x|,\qquad e^{\int Pdx}=\frac{1}{\cos x}=\sec x$$
$$y=e^{-\int P}\Big[\int Qe^{\int P}dx+C\Big]=\cos x\Big[\int\sec x\cdot\sec x\,dx+C\Big]=\cos x\Big[\int\sec^2x\,dx+C\Big]$$
$$=\cos x\big[\tan x+C\big]=\sin x+C\cos x$$

⚠ ① $\int\tan x\,dx=-\ln|\cos x|$（**负号**，常用错）；② $\sec^2x$ 的原函数是 $\tan x$；③ 因为 $-\frac\pi2<x<\frac\pi2$，$\cos x>0$，绝对值可以直接去掉。

## 三、证明题

### P-A1 ★☆☆ 提示

要证 $f'(\xi)-2\xi=0$，把它看成 $\dfrac{d}{dx}\big[f(x)-x^2\big]\Big|_{x=\xi}=0$。

令 $F(x)=f(x)-x^2$。$F$ 在 $[0,1]$ 连续、$(0,1)$ 可导，且
$$F(0)=f(0)-0=0,\qquad F(1)=f(1)-1=0$$
由**罗尔定理**，存在 $\xi\in(0,1)$ 使 $F'(\xi)=0$，即 $f'(\xi)=2\xi$。∎

⚠ **辅助函数构造口诀**：把结论里的 $\xi$ 换成 $x$，移项使右端为 0，左端认成 $F'(x)$，反推 $F$。
本题：$f'(x)-2x=0\Rightarrow F'=f'-2x\Rightarrow F=f-x^2$。

### P-A2 ★☆☆ 提示

取 $g(x)=x^2$。$f,g$ 在 $[a,b]$ 连续、$(a,b)$ 可导，且 $g'(x)=2x\ne0$（因 $a>0$）。

由**柯西中值定理**，存在 $\xi\in(a,b)$ 使
$$\frac{f(b)-f(a)}{g(b)-g(a)}=\frac{f'(\xi)}{g'(\xi)}\ \Longrightarrow\ \frac{f(b)-f(a)}{b^2-a^2}=\frac{f'(\xi)}{2\xi}$$
$$\Longrightarrow\ f(b)-f(a)=\frac{b^2-a^2}{2\xi}f'(\xi)$$

⚠ ① **用柯西必须验证 $g'\ne0$**——这就是题目给 $a>0$ 的原因，写答案时**要把这句话写上**（阅卷按步给分）；② 识别信号："结论里出现 $\frac{f'(\xi)}{\text{某个 }x\text{ 的表达式}}$" ⟹ 柯西，分母那个表达式就是 $g'$。

### P-B1 ★★☆ 提示

令 $F(x)=xf(x)$。$F$ 在 $[a,b]$ 连续、$(a,b)$ 可导。

由**拉格朗日中值定理**，存在 $\xi\in(a,b)$ 使
$$\frac{F(b)-F(a)}{b-a}=F'(\xi)$$
而 $F(b)-F(a)=bf(b)-af(a)$，$F'(x)=f(x)+xf'(x)$，故
$$\frac{bf(b)-af(a)}{b-a}=f(\xi)+\xi f'(\xi)$$

⚠ 识别信号：结论里出现 $\boxed{f+\text{系数}\cdot f'}$ 这种结构，**先想"它是谁的导数"**。本题 $f+\xi f'$ 正是 $(xf)'$，故乘 $x$。
（同类：$f+\frac{1}{x}f'$ 对应 $(xf)'$；$2f+xf'$ 对应 $(x^2f)'$……一般地 $nf+xf'=(x^nf)'$。）

### P-B2 ★★☆ 提示

**第一步：把 $f$ 用 $f'$ 的积分表示。** 由 $f(0)=0$：
$$f(x)=\int_0^xf'(t)\,dt$$
由 **Cauchy 积分不等式**（把被积函数写成 $f'(t)\cdot1$）：
$$f^2(x)=\Big(\int_0^xf'(t)\cdot1\,dt\Big)^2\le\Big(\int_0^x1\,dt\Big)\Big(\int_0^x\big(f'(t)\big)^2dt\Big)=x\int_0^x\big(f'(t)\big)^2dt$$

**第二步：引进 $F(x)=\int_0^xf(t)dt$，用分部积分。**
$$I=\int_0^1f^2dx=\int_0^1f\,dF=\big[fF\big]_0^1-\int_0^1Ff'dx=f(1)F(1)-\int_0^1Ff'dx$$

**第三步：估计 $\int_0^1F^2dx$。** 由第一步的不等式，
$$F(x)=\int_0^xf(t)dt\le\sqrt x\sqrt{\int_0^xf^2}\le\sqrt x\sqrt{I}\ \Longrightarrow\ \int_0^1F^2dx\le\frac12 I$$

**第四步：合并。** 对 $\int_0^1Ff'dx$ 再用 Cauchy 积分不等式：
$$\Big(\int_0^1Ff'dx\Big)^2\le\int_0^1F^2dx\cdot\int_0^1(f')^2dx\le\frac12 I\cdot J$$
又 $I=f(1)F(1)-\int_0^1Ff'dx$，取最坏情形 $f(1)F(1)\le0$，得 $I\le\big|\int_0^1Ff'dx\big|$，于是
$$I^2\le\frac12IJ\ \Longrightarrow\ I\le\frac12J$$
即 $\displaystyle\int_0^1f^2dx\le\frac12\int_0^1(f')^2dx$。∎

⚠ **方法链**（务必记住这条链）：
$$f(x)=\int_0^xf'\ \Longrightarrow\ \text{Cauchy 积分不等式}\ \Longrightarrow\ \int_0^1F^2dx\le\frac12\int_0^1f^2dx\ \Longrightarrow\ \text{合并}$$
等号条件：Cauchy 取等 ⟺ 被积函数成比例，得到微分方程解出 $f$ 的形状。

### P-B3 ★★☆ 提示

**核心技巧：减去一个常数使积分不变。**

因 $\displaystyle\int_0^1\Big(x-\frac12\Big)dx=0$，可以自由减去 $f\big(\frac12\big)$：
$$\int_0^1xf(x)dx-\frac12\int_0^1f(x)dx=\int_0^1\Big(x-\frac12\Big)f(x)dx=\int_0^1\Big(x-\frac12\Big)\Big[f(x)-f\big(\tfrac12\big)\Big]dx$$

**分两段讨论**：
- $x>\frac12$：$x-\frac12>0$，且由 $f$ 单调递增有 $f(x)-f(\frac12)\ge0$，故两项之积 $\ge0$
- $x<\frac12$：$x-\frac12<0$，且 $f(x)-f(\frac12)\le0$，故两项之积 $\ge0$

被积函数处处 $\ge0$，积分 $\ge0$，即
$$\int_0^1xf(x)dx\ge\frac12\int_0^1f(x)dx$$

⚠ 这是**证明积分不等式的通用套路**：① 先把差写成 $\int(\text{权函数})f$；② 用"权函数积分为 0"减去常数 $f(c)$，其中 $c$ 是权函数的零点；③ 分区间讨论，使"权函数与 $f-f(c)$ 同号"。

### P-C1 ★★★ 提示

**第一步：用介值定理找切点。** $f$ 在 $[0,1]$ 连续，$f(0)=0,\ f(1)=1$，故存在 $c\in(0,1)$ 使 $f(c)=\frac12$。

**第二步：在 $[0,c]$ 和 $[c,1]$ 上各用一次拉氏。**

在 $[0,c]$：存在 $\xi\in(0,c)$ 使 $f(c)-f(0)=cf'(\xi)$，即 $\frac12=cf'(\xi)$，故 $\dfrac{1}{f'(\xi)}=2c$。

在 $[c,1]$：存在 $\eta\in(c,1)$ 使 $f(1)-f(c)=(1-c)f'(\eta)$，即 $\frac12=(1-c)f'(\eta)$，故 $\dfrac{1}{f'(\eta)}=2(1-c)$。

**第三步：相加。**
$$\frac{1}{f'(\xi)}+\frac{1}{f'(\eta)}=2c+2(1-c)=2$$

由 $\frac12>0$ 及 $c>0,\ 1-c>0$ 知 $f'(\xi),f'(\eta)\ne0$（可取倒数）；又 $\xi<c<\eta$，故 $\xi\ne\eta$。∎

⚠ ① **"两个不同的中值点" ⟹ 把区间切成两段各用一次中值定理**，切点由介值定理找；② **必须论证 $f'\ne0$ 才能取倒数**（这是阅卷的给分点）；③ 注意 $\xi<c<\eta$ 保证了 $\xi\ne\eta$。

### P-C2 ★★★ 提示

**第一步：把 $f$ 换成 $f''$（两次分部积分，边界项靠 $f(0)=f(1)=0$ 消掉）。**

**第一次**（把 $f$ 看成 $(x-1)'$）：
$$\int_0^1f(x)dx=\Big[(x-1)f(x)\Big]_0^1-\int_0^1(x-1)f'(x)dx=\underbrace{f(0)}_{=0}+\int_0^1(1-x)f'(x)dx=\int_0^1(1-x)f'(x)dx$$

**第二次**（把 $(1-x)$ 看成 $-\dfrac{(1-x)^2}{2}$ 的导数）：
$$\int_0^1(1-x)f'(x)dx=\Big[-\frac{(1-x)^2}{2}f'(x)\Big]_0^1+\int_0^1\frac{(1-x)^2}{2}f''(x)dx=\frac{f'(0)}{2}+\frac12\int_0^1(1-x)^2f''(x)dx$$

这式子里还有 $f'(0)$，**不好**。改用**同时消掉两端**的做法：因 $x(1-x)$ 在 $x=0,1$ 处都是 0，可以作两次分部且边界项全消——

取 $K(x)=-\dfrac{x(1-x)}{2}$（$K(0)=K(1)=0$，$K''=-1$），直接用分部积分验证恒等式：
$$\int_0^1f(x)dx=\int_0^1(-K''(x))f(x)dx=\Big[-K'f\Big]_0^1+\int_0^1K'f'dx=\Big[-K'f+Kf'\Big]_0^1-\int_0^1Kf''dx$$

（$K(0)=K(1)=0$ 使 $[Kf']_0^1=0$；$f(0)=f(1)=0$ 使 $[-K'f]_0^1=0$。）故
$$\boxed{\int_0^1f(x)dx=-\int_0^1K(x)f''(x)dx=-\frac12\int_0^1x(1-x)f''(x)dx}$$

**必须验算这个恒等式**：取 $f(x)=x(1-x)$，则 $f''=-2$，
- 左边 $=\displaystyle\int_0^1x(1-x)dx=\frac12-\frac13=\frac16$
- 右边 $=-\dfrac12\displaystyle\int_0^1x(1-x)\cdot(-2)dx=\int_0^1x(1-x)dx=\frac16$ ✓

**第二步：取绝对值放缩。**
$$\Big|\int_0^1f(x)dx\Big|=\frac12\Big|\int_0^1x(1-x)f''(x)dx\Big|\le\frac12\cdot M\int_0^1x(1-x)dx=\frac{M}{2}\cdot\frac16=\frac{M}{12}$$

∎

⚠ **三个关键**：① 两次分部积分把 $f$ 换成 $f''$，**靠 $f(0)=f(1)=0$ 让两个边界项都消失**；② 得到的核 $-\frac{x(1-x)}{2}$ **一定要拿具体函数验算**（这是唯一能防住符号错的办法）；③ 放缩时把 $|f''|\le M$ 提出来，剩下 $\int_0^1x(1-x)dx=\frac16$ 是纯计算。

> **附**：$x(1-x)$ 在 $[0,1]$ 上最大值为 $\frac14$，用它只能得到较松的界 $\frac M8$。本题的最优常数是 $\frac{1}{12}$，等号在 $f(x)=x(1-x)$ 时取到（此时 $|f''|\equiv2$）。

---
---

# 附一：技巧 → 题号速查表

| 技巧 | 对应题号 |
|---|---|
| 裂项相消求和 | S-A1 |
| Leibniz 判别法（交错级数） | S-A2, S-B2 |
| 比较判别法 + 几何级数放缩 | S-A3, S-C2 |
| 等价无穷小比较（含三角） | S-C1 |
| 比值判别法 | S-B5 |
| 幂级数收敛域**三步写法** | S-A4, S-B1, S-B2, S-C3 |
| 缺项级数（把 $x^2$ 当整体） | S-B2 |
| 幂级数求和（逐项求导/积分） | S-B3, S-B5 |
| 标准展开（$\ln(1+x)$、$\arctan x$） | S-B4 |
| 可分离变量 | E-A1 |
| 齐次方程（$u=y/x$） | E-A2 |
| 一阶线性（公式法） | E-A3, E-B3, E-B4, E-C3 |
| Bernoulli 方程（$z=y^{1-n}$） | E-B5 |
| 可降阶（缺 $y$：$p=y'$） | E-B6 |
| 可降阶（缺 $x$：$y''=p\frac{dp}{dy}$） | E-C2 |
| 二阶常系数（三种特征根） | E-A4, E-B1, E-B2 |
| 待定系数求特解 | E-C1 |
| 辅助函数构造（罗尔） | P-A1 |
| 柯西中值定理（验证 $g'\ne0$） | P-A2 |
| 乘积分因子（$f+xf'=(xf)'$） | P-B1 |
| Cauchy 积分不等式 | P-B2 |
| 减去常数 + 分区间定号 | P-B3 |
| 介值 + 两次拉氏（两个中值点） | P-C1 |
| 两次分部积分造核函数 | P-C2 |

---

# 附二：公式速查卡（考前 10 分钟看）

## 级数判敛

| 方法 | 形式 | 适用信号 |
|---|---|---|
| 比较判别法 | $0\le a_n\le b_n$，$\sum b_n$ 收敛 ⟹ $\sum a_n$ 收敛 | 能放缩（比几何级数/$p$-级数） |
| 比较极限形式 | $\lim\frac{a_n}{b_n}=l\in(0,\infty)$ ⟹ 同敛散 | 能用等价无穷小 |
| 比值判别法 | $\lim\frac{a_{n+1}}{a_n}=\rho$，$\rho<1$ 收敛 | 含阶乘、指数 |
| 根值判别法 | $\lim\sqrt[n]{a_n}=\rho$，同上有 | 含 $n$ 次方 |
| **Leibniz** | $a_n$ 单调减且 $\to0$ ⟹ $\sum(-1)^{n-1}a_n$ 收敛 | 交错级数 |
| 必要条件 | $\sum a_n$ 收敛 ⟹ $a_n\to0$ | 先验算 $a_n\to0$ |

**$p$-级数（基准）**：
$$\sum_{n=1}^\infty\frac{1}{n^p}\ \begin{cases}\text{收敛},&p>1\\[2pt]\text{发散},&p\le1\end{cases}\qquad
\sum_{n=2}^\infty\frac{1}{n\ln^qn}\ \begin{cases}\text{收敛},&q>1\\[2pt]\text{发散},&q\le1\end{cases}$$

## 收敛半径与端点

$$R=\lim_{n\to\infty}\Big|\frac{a_n}{a_{n+1}}\Big|\qquad\text{或}\qquad R=\frac{1}{\lim\sqrt[n]{|a_n|}}$$

**三步写法（必写全）**：① 算 $R$；② 判 $x=R$；③ 判 $x=-R$。**两个端点结论可能不同**。

## 必背展开式

$$\frac{1}{1-x}=\sum_{n=0}^\infty x^n\ (|x|<1),\qquad e^x=\sum_{n=0}^\infty\frac{x^n}{n!}\ (x\in\mathbb R)$$
$$\ln(1+x)=\sum_{n=1}^\infty\frac{(-1)^{n-1}}{n}x^n\ \ (-1<x\le1),\qquad \sin x=\sum_{n=0}^\infty\frac{(-1)^n}{(2n+1)!}x^{2n+1}$$
$$\arctan x=\sum_{n=0}^\infty\frac{(-1)^n}{2n+1}x^{2n+1}\ \ (|x|\le1)$$

**常用和的结论**：$\displaystyle\sum_{n=1}^\infty\frac{1}{n^2}=\frac{\pi^2}{6}$，$\displaystyle\sum_{n=1}^\infty\frac{(-1)^{n-1}}{n}=\ln2$，$\displaystyle\sum_{n=1}^\infty\frac{1}{n2^n}=\ln2$

**幂级数求和的两个动作**：
$$\sum_{n=0}^\infty x^n=\frac{1}{1-x}\ \xrightarrow{\ \text{求导}\ }\ \sum_{n=1}^\infty nx^{n-1}=\frac{1}{(1-x)^2}\ \xrightarrow{\ \times x\ }\ \sum_{n=1}^\infty nx^n=\frac{x}{(1-x)^2}$$

## 微分方程

| 类型 | 标准形式 | 解法 |
|---|---|---|
| 可分离变量 | $\dfrac{dy}{dx}=f(x)g(y)$ | 分离：$\dfrac{dy}{g(y)}=f(x)dx$ |
| 齐次方程 | $\dfrac{dy}{dx}=f\!\left(\dfrac yx\right)$ | 令 $u=\dfrac yx$，$y=ux,\ y'=u+xu'$ |
| **一阶线性** | $y'+P(x)y=Q(x)$ | $y=e^{-\int P}\Big[\int Qe^{\int P}dx+C\Big]$ |
| **Bernoulli** | $y'+P(x)y=Q(x)y^n$ | 令 $z=y^{1-n}$ 化为线性 |
| 可降阶（缺 $y$） | $y''=f(x,y')$ | 令 $p=y'$，则 $y''=p'$ |
| 可降阶（缺 $x$） | $y''=f(y,y')$ | 令 $p=y'$，则 $y''=p\dfrac{dp}{dy}$ |
| 二阶常系数 | $y''+py'+qy=f(x)$ | 通解 = 齐次通解 + 一个特解 |

**二阶常系数齐次三种情形**：

| 判别式 | 根 | 通解 |
|---|---|---|
| $p^2-4q>0$ | $r_1\ne r_2$ | $C_1e^{r_1x}+C_2e^{r_2x}$ |
| $p^2-4q=0$ | $r$ 二重 | $(C_1+C_2x)e^{rx}$ |
| $p^2-4q<0$ | $\alpha\pm\beta i$ | $e^{\alpha x}(C_1\cos\beta x+C_2\sin\beta x)$ |

**待定系数（$f(x)=P_m(x)e^{\lambda x}$）**：设 $y^*=x^kQ_m(x)e^{\lambda x}$，其中 $k=$ $\lambda$ 作为特征根的重数（0、1、2）。

## 证明题三大武器

**武器一：构造辅助函数 + 罗尔/拉氏**
$$\text{口诀：把 }\xi\text{ 换成 }x\text{，移项使右端为 0，左端认成 }F'(x)\text{，反推 }F$$

**武器二：Cauchy 积分不等式**
$$\Big(\int_a^bfg\,dx\Big)^2\le\int_a^bf^2dx\cdot\int_a^bg^2dx$$

**武器三：化为二重积分 + 分区域定号**
$$\int_a^bf(x)\Big(\int_a^xg(s)ds\Big)dx=\iint_{a\le s\le x\le b}f(x)g(s)\,ds\,dx$$

**常用导数结构识别**：
$$f+\text{系数}\cdot f'\ \Longleftrightarrow\ (x^nf)'\ \text{型（乘积分因子）}$$
$$\frac{f'g-fg'}{g^2}\ \Longleftrightarrow\ \Big(\frac fg\Big)'$$

---

# 附三：高频易错点清单（考前自检）

| # | 易错点 | 对应题 |
|---|---|---|
| 1 | **忘记验算 $a_n\to0$**（收敛的必要条件） | S-A2 |
| 2 | **把"收敛"当成"绝对收敛"** | S-A2 |
| 3 | **$p$-级数在 $p=1$ 时误判收敛**（调和级数发散） | S-A2 |
| 4 | **幂级数只写半径不判端点** | S-A4、S-B1、S-C3 |
| 5 | **缺项级数硬套 $R$ 公式** | S-B2 |
| 6 | **$\ln(1+x)$ 展开的收敛域写成 $[-1,1]$**（应为 $(-1,1]$） | S-B4 |
| 7 | **求和时结构没凑对**（少一个 $x$ 因子） | S-B3、S-B5 |
| 8 | **一阶线性不先化成标准形式** | E-A3、E-C3 |
| 9 | **分离变量丢了解**（如 $y\equiv0$） | E-A1 |
| 10 | **二阶重根不补 $x$** | E-B2 |
| 11 | **$\int\tan x\,dx$ 丢负号** | E-C3 |
| 12 | **缺 $y$ 与缺 $x$ 的降阶公式混用** | E-B6、E-C2 |
| 13 | **待定系数前不检查 $\lambda$ 是不是特征根** | E-C1 |
| 14 | **用柯西定理不验证 $g'\ne0$** | P-A2 |
| 15 | **辅助函数构造不出**（背口诀） | P-A1 |
| 16 | **积分不等式不做"减常数"处理** | P-B3 |
| 17 | **取倒数前不论证 $f'\ne0$** | P-C1 |
| 18 | **分部积分造核后不验算** | P-C2 |

---

# 自测评分标准

| 结果 | 判断 | 下一步 |
|---|---|---|
| A 组全对，B 组对 80%+ | 阶段三过关 | 进入真题模拟 |
| S 组错 ≥3 题 | 判敛法不熟 | 重背 $p$-级数结论 + 做 S-A2～S-B2，把"收敛域三步写法"默写三遍 |
| E 组错 ≥3 题 | 方程类型识别慢 | 背类型识别表：看到什么结构用什么换元 |
| P 组写不出辅助函数 | 套路没内化 | 把 P-A1、P-B1 的构造口诀抄成卡片，隔天默写；P-B2 的方法链背下来 |
| C 组全错 | 正常 | C 组是拉分题，二刷时重点攻 S-C2、E-C2、P-C2 |
