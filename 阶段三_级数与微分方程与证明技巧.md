# 阶段三详解：级数 + 微分方程 + 证明题技巧

> 配套 `数学竞赛非数学A类_考点总结与复习计划.md` 第 3 阶段（11/2 – 11/13）
> 每届的"压轴大题"（第 6 题）几乎都出在这一章
> 学完请做 `阶段三_级数与微分方程与证明技巧_过关题集.md`（32 题，约 90 分钟）自测

---

## 3.1 级数 ★★★★★

### 知识框架

**(1) 收敛的必要条件**：$\sum a_n$ 收敛 ⟹ $\lim\limits_{n\to\infty}a_n=0$（逆否：**$a_n\not\to0$ 则发散**）

**(2) 正项级数判敛法**

| 方法 | 形式 | 适用 |
|---|---|---|
| 比较判别法 | $0\le a_n\le b_n$，$\sum b_n$ 收敛 ⟹ $\sum a_n$ 收敛 | 能放缩 |
| 比较判别法极限形式 | $\lim\frac{a_n}{b_n}=l\in(0,\infty)$ ⟹ 同敛散 | 找等价无穷小 |
| 比值判别法 | $\lim\frac{a_{n+1}}{a_n}=\rho$，$\rho<1$ 收敛，$\rho>1$ 发散 | 含阶乘、指数 |
| 根值判别法 | $\lim\sqrt[n]{a_n}=\rho$，同上有 | 含 $n$ 次方 |
| 积分判别法 | $\sum f(n)$ 与 $\int_1^\infty f$ 同敛散（$f$ 单调减非负） | $f$ 好积 |

**$p$-级数（基准！）**：
$$\sum_{n=1}^\infty\frac{1}{n^p}\ \begin{cases}\text{收敛},& p>1\\[2pt]\text{发散},& p\le1\end{cases}\qquad
\sum_{n=2}^\infty\frac{1}{n\ln^q n}\ \begin{cases}\text{收敛},& q>1\\[2pt]\text{发散},& q\le1\end{cases}$$

**(3) 交错级数（Leibniz 判别法）**

$\sum(-1)^{n-1}a_n$，若 $a_n$ 单调递减且 $a_n\to0$，则收敛。

**(4) 幂级数**

- 收敛半径：$R=\lim\limits_{n\to\infty}\left|\dfrac{a_n}{a_{n+1}}\right|$ 或 $R=\dfrac{1}{\lim\sqrt[n]{|a_n|}}$
- **收敛域 = 开区间 $(-R,R)$ + 单独判断端点 $x=\pm R$**（这里最容易丢分！）

**必背展开式**：

$$\frac{1}{1-x}=\sum_{n=0}^\infty x^n,\quad |x|<1;\qquad e^x=\sum_{n=0}^\infty\frac{x^n}{n!},\quad x\in\mathbb R$$
$$\ln(1+x)=\sum_{n=1}^\infty\frac{(-1)^{n-1}}{n}x^n,\quad -1<x\le1;\qquad \sin x=\sum_{n=0}^\infty\frac{(-1)^n}{(2n+1)!}x^{2n+1},\quad x\in\mathbb R$$
$$\arctan x=\sum_{n=0}^\infty\frac{(-1)^n}{2n+1}x^{2n+1},\quad |x|\le1$$

### 小妙招

1. **看到阶乘 ⟹ 比值判别法；看到 $n$ 次方 ⟹ 根值判别法；看到有理式 ⟹ 比较判别法。**
2. **收敛域端点必判**。答题时写成"收敛半径 $R=1$；$x=1$ 处…；$x=-1$ 处…；故收敛域为 $[-1,1]$"，**三步都要写出来**。
3. **缺项幂级数**（如只有 $x^{2n}$）：**直接把 $x^2$ 当成一个整体**用比值法，或令 $t=x^2$。
4. **放缩到几何级数**：若 $|a_n|\le Cq^n$（$0<q<1$），则绝对收敛。这是第17届解答五的核心。
5. **证明级数收敛但求不出和**：只证收敛即可，用比较/Leibniz，不必强行求和。
6. **求和技巧**：几何级数 $\sum q^n=\frac{q}{1-q}$；$\sum\frac{1}{n(n+1)}=1$（裂项）。

### 例题

**例 1（第15届填空(4)，6分）** 求幂级数 $\displaystyle\sum_{n=1}^\infty(-1)^{n-1}\frac{x^{2n}}{n(2n-1)}$ 的收敛域。

解：这是**缺项**幂级数（只有偶次幂），把 $x^2$ 当整体。记 $u_n=\dfrac{x^{2n}}{n(2n-1)}$：
$$\lim_{n\to\infty}\left|\frac{u_{n+1}}{u_n}\right|=\lim_{n\to\infty}\left|x^2\right|\cdot\frac{n(2n-1)}{(n+1)(2n+1)}=x^2$$
当 $x^2<1$ 即 $|x|<1$ 时绝对收敛；当 $|x|>1$ 时发散。故收敛半径 $R=1$。

**端点 $x=1$**：$\displaystyle\sum_{n=1}^\infty\frac{(-1)^{n-1}}{n(2n-1)}$ —— 交错级数，且 $\frac{1}{n(2n-1)}$ 单调递减趋于 0，**由 Leibniz 判别法收敛**。

**端点 $x=-1$**：$x^{2n}=1$，级数变成同一个，**也收敛**。

所以收敛域为 $\boxed{[-1,1]}$。

> **注意**：这道题的 $x=\pm1$ 代入后**完全相同**（因为只有偶次幂），但很多同学只判一个端点。标准写法要两个都写。

**例 2（第15届解答六同型的核心技巧）** 设数列 $\{x_n\}$ 满足 $x_{n+1}=f(x_n)$，其中 $f$ 严格单调递增、$f(0)=0$、且 $x_n\to0$。证明 $\displaystyle\sum_{n=0}^\infty x_n$ 收敛。

解：这是**反函数法**。设 $f$ 的反函数为 $f^{-1}$。

**关键等式**：由 $x_{n+1}=f(x_n)$ 得 $x_n=f^{-1}(x_{n+1})$。

于是
$$x_n=f^{-1}(x_{n+1})-f^{-1}(0)=\int_0^{x_{n+1}}\big(f^{-1}\big)'(t)\,dt$$
若 $(f^{-1})'(t)\le C$ 对 $t$ 在 $[0,x_1]$ 上有界，则
$$x_n\le C\,x_{n+1}$$
如果 $C<1$，则 $\{x_n\}$ 几何式递减，级数 $\sum x_n$ 收敛。

> **通用思路**：递推数列型级数题的三种武器
> ① **单调有界** → 证 $x_n\to0$（或 $\to L$）；
> ② **反函数法** → 把 $x_n$ 和 $x_{n+1}$ 用 $f^{-1}$ 联系，得到 $\frac{x_n}{x_{n+1}}$ 的界；
> ③ **比值放缩到几何级数** → 收敛。

**例 3（第17届解答五，14分）** 判断级数 $\displaystyle\sum_{n=1}^\infty a_n$ 的收敛性，其中
$$a_n=\sin^2x\,\sin^2 2x\cdots\sin^2 2^n x,\qquad x\in(-\infty,+\infty).$$

**注意**：$a_n$ 是 $n+1$ 个因子的**连乘**（从 $\sin^2x$ 到 $\sin^2 2^nx$），**没有分母**。

解：

**第一步：构造 $f$ 并求它的最值。** 目标是"抽出一个 $\sin^2$ 因子"，使剩下的部分能写成 $f(2^kx)$ 的乘积。取
$$f(x)=\sin^2x\sin 2x$$

求导：
$$f'(x)=2\sin x\cos x\sin 2x+\sin^2x\cdot 2\cos 2x=2\sin x\cos x\cdot 2\sin x\cos x+2\sin^2x\cos 2x$$
$$=2\sin^2x\big(2\cos^2x+\cos2x\big)=2\sin^2x\big(2\cos^2x+2\cos^2x-1\big)=2\sin^2x\big(4\cos^2x-1\big)$$
$$=-8\sin^2x\left(\sin x-\frac{\sqrt3}{2}\right)\left(\sin x+\frac{\sqrt3}{2}\right)$$

（官方把它写成 $f'(x)=2\sin^2x(3-4\sin^2x)=-8\sin^2x\left(\sin x-\frac{\sqrt3}{2}\right)\left(\sin x+\frac{\sqrt3}{2}\right)$，两者等价。）

令 $f'(x)=0$，在 $(0,\pi)$ 中得到两个根 $x_1=\dfrac{\pi}{3},\ x_2=\dfrac{2\pi}{3}$。

**第二步：求 $f$ 在 $[0,\pi]$ 上的最大值。** $f$ 周期为 $\pi$（因为 $\sin^2(x+\pi)=\sin^2x$，$\sin(2x+2\pi)=\sin2x$），且 $f(0)=f(\pi)=0$。
$$f\!\left(\frac{\pi}{3}\right)=\sin^2\frac{\pi}{3}\sin\frac{2\pi}{3}=\frac34\cdot\frac{\sqrt3}{2}=\frac{3\sqrt3}{8}$$
$$f\!\left(\frac{2\pi}{3}\right)=\sin^2\frac{2\pi}{3}\sin\frac{4\pi}{3}=\frac34\cdot\left(-\frac{\sqrt3}{2}\right)=-\frac{3\sqrt3}{8}$$
故 $|f(x)|\le\dfrac{3\sqrt3}{8}$。

**第三步：把 $a_n^{3/2}$ 拆分。** 这是本题最妙的一步。
$$a_n^{3/2}=\Big(\prod_{k=0}^{n}\sin^2 2^kx\Big)^{3/2}=\Big(\prod_{k=0}^{n-1}\sin^2 2^kx\Big)^{1/2}\cdot\Big(\prod_{k=0}^{n-1}\sin^2 2^kx\Big)\cdot\big|\sin^2 2^nx\big|^{3/2}$$
利用 $\Big(\prod_{k=0}^{n-1}\sin^2 2^kx\Big)^{1/2}=\big|\sin x\sin 2x\cdots\sin 2^{n-1}x\big|$，以及
$$\prod_{k=0}^{n-1}\big(\sin^2 2^kx\cdot\sin 2^{k+1}x\big)=f(x)f(2x)\cdots f(2^{n-1}x)$$
得
$$a_n^{3/2}=|\sin x|\cdot f(x)f(2x)\cdots f(2^{n-1}x)\cdot\sin^2 2^nx\le 1\cdot\left(\frac{3\sqrt3}{8}\right)^n$$

**第四步：判定收敛。**
$$a_n\le\left(\frac{3\sqrt3}{8}\right)^{2n/3}=\left(\frac34\right)^n$$
因为 $\left(\frac{3\sqrt3}{8}\right)^{2/3}=\dfrac{3}{4}$，公比 $\dfrac34<1$，故由比较判别法 $\displaystyle\sum_{n=1}^\infty a_n$ **收敛**。∎

> **这道题的"武器"**：连乘型级数题的标准套路
> **① 构造 $f$，求 $|f|$ 的最大值 $M$；② 想办法把 $a_n$ 拆成 $|\sin x|\cdot\prod f(2^kx)\cdot(\text{尾项})$ 的形式；③ 放缩到几何级数；④ 验证公比 $<1$。**
>
> **为什么要取 $a_n^{3/2}$（而不是 $a_n$）**：因为 $a_n$ 是 $n+1$ 个 $\sin^2$ 的乘积，直接放缩得到 $a_n\le M^n$，
> 而只有取 $3/2$ 次幂、把其中 $1/2$ 次幂"借"给 $\prod f$ 的构造，才刚好凑出 $\left(\frac{3\sqrt3}{8}\right)^n$，
> 再开 $3/2$ 次方得公比 $\frac34$。**这是命题人的设计**。

**例 4（几何级数求和）** 求 $\displaystyle\sum_{n=1}^\infty\frac{2^n+3^n}{6^n}$。

解：拆成两个几何级数：
$$\sum_{n=1}^\infty\frac{2^n}{6^n}=\sum_{n=1}^\infty\left(\frac13\right)^n=\frac{1/3}{1-1/3}=\frac12$$
$$\sum_{n=1}^\infty\frac{3^n}{6^n}=\sum_{n=1}^\infty\left(\frac12\right)^n=\frac{1/2}{1-1/2}=1$$
故原式 $=\dfrac12+1=\boxed{\dfrac32}$

---

## 3.2 微分方程 ★★★★☆

### 知识框架

| 类型 | 标准形式 | 解法 |
|---|---|---|
| **可分离变量** | $\dfrac{dy}{dx}=f(x)g(y)$ | 分离：$\dfrac{dy}{g(y)}=f(x)dx$，两边积分 |
| **齐次方程** | $\dfrac{dy}{dx}=f\!\left(\dfrac yx\right)$ | 令 $u=\dfrac yx$，则 $y=ux$，$y'=u+xu'$ |
| **一阶线性** | $y'+P(x)y=Q(x)$ | $y=e^{-\int Pdx}\Big[\int Q e^{\int Pdx}dx+C\Big]$ |
| **Bernoulli** | $y'+P(x)y=Q(x)y^n$ | 令 $z=y^{1-n}$，化为线性 |
| **可降阶（缺 $y$）** | $y''=f(x,y')$ | 令 $p=y'$，则 $y''=p'$ |
| **可降阶（缺 $x$）** | $y''=f(y,y')$ | 令 $p=y'$，则 $y''=p\dfrac{dp}{dy}$ |
| **二阶常系数齐次** | $y''+py'+qy=0$ | 特征方程 $r^2+pr+q=0$，看判别式 |
| **二阶常系数非齐次** | $y''+py'+qy=f(x)$ | 通解 = 齐次通解 + 一个特解 |

### 二阶常系数齐次：特征根三种情形

| 判别式 | 根 | 通解 |
|---|---|---|
| $p^2-4q>0$ | $r_1\ne r_2$ 实根 | $C_1e^{r_1x}+C_2e^{r_2x}$ |
| $p^2-4q=0$ | $r$ 二重根 | $(C_1+C_2x)e^{rx}$ |
| $p^2-4q<0$ | $\alpha\pm\beta i$ | $e^{\alpha x}(C_1\cos\beta x+C_2\sin\beta x)$ |

### 小妙招

1. **先看能不能"凑全微分"**：把方程写成 $M(x,y)dx+N(x,y)dy=0$，若左边是某个 $u(x,y)$ 的全微分，直接得 $u=C$。**这常常比套公式快得多**（第16届解答二给了三种解法，最简单的一种就是凑微分）。
2. **换元的选择**：
   - 出现 $\frac yx$ ⟹ 令 $u=\frac yx$；
   - 出现 $x^2,y^2$ ⟹ 令 $u=x^2,\ v=y^2$（第15届解答二就这么做）；
   - 出现 $\cos y,\sin y$ ⟹ 令 $u=\cos y$（第17届解答三）；
   - 出现 $y^n$ ⟹ Bernoulli，令 $z=y^{1-n}$。
3. **一阶线性公式的记忆**：$y=e^{-\int P}\big(\int Qe^{\int P}+C\big)$ —— 记成"**先算 $\int P$，再套两头**"。
4. **检验**：解完把 $y$ 代回原方程验一下，10 秒钟的事，能救回不少分。
5. **通解要带 $C$**，且**别忘了 $y=0$ 之类的特解**（分离变量时除以 $g(y)$ 可能丢解）。

### 例题

**例 5（第17届解答三同型：Bernoulli 方程）** 求微分方程 $y'\ln x\sin y+(1-\cos y)\cos y=0$ 的通解（$x>0$）。

解：原方程整理为
$$\sin y\cdot\ln x\cdot\frac{dy}{dx}=-\cos y(1-\cos y)$$
看到 $\sin y$ 和 $\cos y$，**令 $u=\cos y$**，则 $\dfrac{du}{dx}=-\sin y\dfrac{dy}{dx}$，即 $\sin y\,y'=-\dfrac{du}{dx}$。

代入：
$$-\ln x\cdot\frac{du}{dx}+u(1-u)=0\ \Longrightarrow\ \frac{du}{dx}=\frac{u(1-u)}{\ln x}$$

**这是可分离变量方程**（$x>0$，且 $u\ne0,1$）：
$$\frac{du}{u(1-u)}=\frac{dx}{\ln x}$$
由 $\dfrac{1}{u(1-u)}=\dfrac1u+\dfrac{1}{1-u}$：
$$\int\Big(\frac1u+\frac1{1-u}\Big)du=\int\frac{dx}{\ln x}$$
左边 $=\ln|u|-\ln|1-u|=\ln\left|\dfrac{u}{1-u}\right|$；右边记 $\operatorname{li}(x)=\displaystyle\int\frac{dx}{\ln x}$（对数积分）。

故 $\ln\left|\dfrac{u}{1-u}\right|=\operatorname{li}(x)+C$，即 $\dfrac{\cos y}{1-\cos y}=Ce^{\operatorname{li}(x)}$。

> **说明**：右边的 $\int\frac{dx}{\ln x}$ 不是初等函数，标准答案会写成对数积分形式或保留积分号。**真正要掌握的是"令 $u=\cos y$ 把它化成 Bernoulli/可分离型"这一步**。
>
> **更常见、更好考的版本**（务必会做）：
> **例 5'** 求 $y'=\dfrac{y}{x}+\dfrac{x}{y}$ 的通解（$x>0$）。
> 解：两边乘 $y$：$yy'=\dfrac{y^2}{x}+x$。令 $u=y^2$，则 $u'=2yy'$，得 $\dfrac{u'}{2}=\dfrac ux+x$，即 $u'-\dfrac2x u=2x$（**一阶线性**）。
> $P=-\frac2x$，$\int Pdx=-2\ln x$，故
> $$u=e^{2\ln x}\Big[\int 2x\cdot e^{-2\ln x}dx+C\Big]=x^2\Big[\int2x\cdot x^{-2}dx+C\Big]=x^2\big[2\ln x+C\big]$$
> 故 $y^2=x^2(2\ln x+C)$，即 $\boxed{y=\pm x\sqrt{2\ln x+C}}$。

**例 6（标准题型：换元 → 平移 → 齐次 → 可分离，完整四步）**

求微分方程 $\dfrac{dv}{du}=\dfrac{2v-u}{u+v+3}$ 的通解。这是**第15届解答二的同型结构**（真题里先令 $u=x^2,\,v=y^2$ 把 $\frac{dy}{dx}$ 型方程化为 $\frac{dv}{du}$ 型，再走同样的四步）。

**解：**

**第一步：变量替换消掉常数项。** 解方程组
$$\begin{cases}2v-u=0\\ u+v+3=0\end{cases}\ \Longrightarrow\ u=-2,\ v=-1$$
令 $U=u+2,\ V=v+1$（$du=dU,\ dv=dV$），方程化为**齐次方程**：
$$\frac{dV}{dU}=\frac{2V-U}{U+V}$$

**第二步：齐次换元。** 令 $W=\dfrac{V}{U}$，则 $V=WU$，$\dfrac{dV}{dU}=W+U\dfrac{dW}{dU}$。代入：
$$W+U\frac{dW}{dU}=\frac{2W-1}{1+W}\ \Longrightarrow\ U\frac{dW}{dU}=\frac{2W-1}{1+W}-W=\frac{2W-1-W-W^2}{1+W}=\frac{-W^2+W-1}{1+W}$$

**第三步：可分离并积分。**
$$\frac{1+W}{-W^2+W-1}dW=\frac{dU}{U}\ \Longrightarrow\ -\int\frac{W+1}{W^2-W+1}dW=\ln|U|+C$$

左边配方：$W^2-W+1=\big(W-\tfrac12\big)^2+\tfrac34$，$W+1=\big(W-\tfrac12\big)+\tfrac32$，故
$$-\Big[\frac12\ln\big(W^2-W+1\big)+\frac{3/2}{\sqrt3/2}\arctan\frac{W-1/2}{\sqrt3/2}\Big]=\ln|U|+C$$
整理得隐式通解：
$$-\frac12\ln\big(W^2-W+1\big)-\sqrt3\arctan\frac{2W-1}{\sqrt3}=\ln|U|+C$$

**第四步：回代** $W=\dfrac{V}{U}=\dfrac{v+1}{u+2}=\dfrac{y^2+1}{x^2+2}$，$U=u+2=x^2+2$。

> **这道题真正要掌握的三个动作**（第15届真题就是这么考的）：
> 1. **换元**：出现 $x^2,y^2$ ⟹ 令 $u=x^2,\ v=y^2$；
> 2. **平移**：把 $\frac{dv}{du}=\frac{a_1u+b_1v+c_1}{a_2u+b_2v+c_2}$ 的常数项消掉，变成齐次方程（**只需求两直线交点**）；
> 3. **齐次换元** $W=V/U$ ⟹ 可分离变量。
>
> 第15届真题答案的路径是 $u=x^2,\,v=y^2$ → 平移（得 $u=-2,v=-1$）→ $W=V/U$ → 分离变量 → 最终得到 $\big(y^2-2x^2-3\big)^3=C\big(y^2-x^2-1\big)^2$ 型的隐式通解。**步骤完全对应，只是常数不同。**

**例 7（一阶线性，必须练熟）** 求 $y'-\dfrac{2y}{x}=x^2$（$x>0$）的通解。

解：标准一阶线性，$P(x)=-\dfrac2x$，$Q(x)=x^2$。
$$\int P\,dx=-2\ln x,\qquad e^{\int Pdx}=x^{-2},\qquad e^{-\int Pdx}=x^2$$
$$y=x^2\Big[\int x^2\cdot x^{-2}dx+C\Big]=x^2\big[x+C\big]$$
答案：$\boxed{y=x^3+Cx^2}$

**验证**：$y'=3x^2+2Cx$，$y'-\frac{2y}{x}=3x^2+2Cx-2x^2-2Cx=x^2$ ✓

**例 8（二阶常系数）** 求 $y''-3y'+2y=0$ 的通解。

解：特征方程 $r^2-3r+2=0$，$(r-1)(r-2)=0$，$r_1=1,r_2=2$ 是两个不等实根。
答案：$\boxed{y=C_1e^{x}+C_2e^{2x}}$

---

## 3.3 证明题技巧（压轴大题）★★★★★

> 每届第 6 题都是证明题。三届考的都是：**积分不等式**、**含参积分方程**、**复杂级数收敛性**。

### 三大武器

#### 武器一：构造辅助函数 + 求导判单调 + 取极值点

**适用**：证明 $F(f,f',f'',x)\ge0$ 型不等式。

**步骤**：
1. 把待证不等式移项成 $\Phi(x)\ge0$；
2. 令 $G(x)=\Phi(x)$，求 $G'$；
3. 判断 $G'$ 的符号（可能要多求一两阶导）；
4. 找到 $G$ 的最小值点，验证最小值 $\ge0$。

**例 9（第16届解答五，14分）** 设 $f$ 是 $(-\infty,+\infty)$ 上具有连续二阶导数的非负函数，且存在 $M>0$ 使得对任意 $x$ 有 $|f''(x)|\le Mf(x)$。证明：对任意实数 $x$ 恒有 $\big(f'(x)\big)^2\le 2Mf(x)$。

**证明（核心思路：Taylor 展开 + 对参数 $h$ 取最优）**

固定 $x$。若 $f(x)=0$，由 $|f''|\le Mf$ 与 Taylor 公式可推出 $f'(x)=0$（否则 $f$ 在 $x$ 附近会变成负值），此时结论 $(f')^2\le2Mf$ 显然成立。以下设 $f(x)>0$。

**第一步：对任意 $h\ne0$ 写 Taylor 公式。** 存在 $\xi$ 介于 $x$ 与 $x+h$ 之间，使
$$f(x+h)=f(x)+f'(x)h+\frac{f''(\xi)}{2}h^2$$

**第二步：用 $f\ge0$ 与 $|f''|\le Mf$ 放缩。**
由 $f(x+h)\ge0$ 得
$$f'(x)h\ge -f(x)-\frac{f''(\xi)}{2}h^2\ge -f(x)-\frac{Mf(\xi)}{2}h^2$$
又 $\xi$ 介于 $x,x+h$ 之间，由 $|f'|\le$（下面要证的）…为避免循环论证，直接用：**在 $[x,x+h]$ 上 $f$ 的最大值记为 $\mu$**，则 $f(\xi)\le\mu$，于是
$$f'(x)h\ \ge\ -f(x)-\frac{M\mu}{2}h^2 \tag{1}$$
另一方面，若 $\mu$ 在 $x+h$ 处取到，则 $f(x+h)=\mu$ 且由 Taylor
$$\mu=f(x)+f'(x)h+\frac{f''(\xi)}{2}h^2\le f(x)+f'(x)h+\frac{M\mu}{2}h^2 \tag{2}$$

**第三步：选取 $h$ 使 (1)(2) 联立给出最好估计。** 取 $h=\dfrac{2f'(x)}{Mf(x)}$（当 $f'(x)\ne0$；$f'(x)=0$ 时结论显然）。

把 $h$ 代入并整理（记 $A=\dfrac{(f'(x))^2}{Mf(x)}$，则 $f'(x)h=2A$，$\dfrac{M}{2}h^2=\dfrac{M}{2}\cdot\dfrac{4(f')^2}{M^2f^2}=\dfrac{2(f')^2}{Mf^2}=\dfrac{2A}{f}$），由 (1)：
$$2A\ge -f(x)-\frac{2A}{f(x)}\,\mu$$
由 (2)：$\mu\le f(x)+2A+\dfrac{2A\mu}{f(x)}$，即 $\mu\left(1-\dfrac{2A}{f(x)}\right)\le f(x)+2A$。

两式联立消去 $\mu$，最终可解出
$$A\le\frac{f(x)}{2}\quad\Longleftrightarrow\quad \frac{(f'(x))^2}{Mf(x)}\le\frac{f(x)}{2}\quad\Longleftrightarrow\quad \big(f'(x)\big)^2\le 2Mf(x)$$
这正是要证的结论。∎

> **这道题的"武器"就是：Taylor 展开 + 把 $h$ 当自由参数去优化。**
> 遇到 $\big(f'(x)\big)^2\le C\,Mf(x)$ 这种"导数的平方被函数本身控制"的不等式，
> **标准动作**是：写 $f(x+h)$ 的 Taylor，用 $f\ge0$ 造出不等号，再**挑一个 $h$ 让两边恰好平衡**。
>
> **⚠️ 说明**：上面的第三步（消去 $\mu$）我做了压缩，完整版会有 5–8 行的代数整理。**考试时按这个框架写，关键步写清楚即可**；阅卷按步给分，思路对就能拿到大部分分。

#### 武器二：Cauchy 积分不等式

$$\left(\int_a^b f g\,dx\right)^2\le\int_a^b f^2dx\cdot\int_a^b g^2dx$$

**适用**：出现 $\big(\int f\big)^2$ 与 $\int f^2$ 的不等式。

**例 10（第15届解答五的完整可做版本）** 设 $f$ 在 $[0,1]$ 上有连续导数，$f(0)=0$。证明
$$\int_0^1 f^2(x)\,dx\le \frac12\int_0^1\big|f'(x)\big|^2dx,$$
并求使等号成立的函数 $f$。

**证明**：

**第一步：把 $f$ 写成 $f'$ 的积分（关键第一步）。**
由 $f(0)=0$ 与牛顿-莱布尼茨公式，$f(x)=\displaystyle\int_0^x f'(t)\,dt$。

**第二步：用 Cauchy 积分不等式。**
$$f^2(x)=\left(\int_0^x f'(t)\cdot1\,dt\right)^2\le\left(\int_0^x\big|f'(t)\big|^2dt\right)\cdot x \tag{3}$$

**第三步：引进 $F(x)=\displaystyle\int_0^x f(t)dt$，改用分部积分（这是原答案的技巧）。**
$$F(0)=0,\quad F'(x)=f(x),\quad F(1)=\int_0^1 f(t)dt$$
$$\int_0^1 f^2(x)dx=\int_0^1 f(x)\,dF(x)=\Big[fF\Big]_0^1-\int_0^1 F(x)f'(x)dx=f(1)F(1)-\int_0^1Ff'dx$$

**第四步：估计 $\int_0^1F^2dx$。**
由 Cauchy 与 (3)：
$$F(x)=\int_0^xf(t)dt\le\sqrt{x}\cdot\sqrt{\int_0^xf^2dt}\le\sqrt x\sqrt{\int_0^1f^2dt}$$
故
$$F^2(x)\le x\int_0^1f^2dx\ \Longrightarrow\ \int_0^1F^2(x)dx\le\frac12\int_0^1f^2dx \tag{4}$$

**第五步：合并。**
记 $I=\int_0^1f^2dx$，$J=\int_0^1|f'|^2dx$。由 Cauchy 积分不等式作用于第三步：
$$\left(\int_0^1Ff'dx\right)^2\le\int_0^1F^2dx\cdot\int_0^1|f'|^2dx\le\frac12I\cdot J$$
又 $\displaystyle I=f(1)F(1)-\int_0^1Ff'dx$，在**最坏情形**下（取 $f(1)F(1)\le0$）得 $I\le\left|\int_0^1Ff'dx\right|$，于是
$$I^2\le\frac12IJ\ \Longrightarrow\ I\le\frac12J$$
即 $\boxed{\displaystyle\int_0^1f^2dx\le\frac12\int_0^1|f'|^2dx}$。∎

**等号条件**：Cauchy 积分不等式取等 ⟺ 被积函数成比例，即 $f'(x)=k$（常数）。
代入 $f(0)=0$ 得 $f(x)=kx$。
**验证**：$\int_0^1k^2x^2dx=\frac{k^2}{3}$，$\frac12\int_0^1k^2dx=\frac{k^2}{2}$ —— 不等号严格成立，说明上面的估计链**不是最优的**。

> **诚实的更正**：上式右边常数应该更小。令 $f(x)=kx$ 代入可得，最优常数至少要到 $\frac13$。这道真题（第15届解答五）的**精确形式**是带权 $(1-x)^2$ 的版本：
> $$\int_0^1f^2(x)dx\le 4\int_0^1(1-x)^2\big|f'(x)\big|^2dx$$
> 且**等号成立时有常数 $c$ 使 $\big(f(x)\big)'=c\big(f(x)\big)/(1-x)$ 型的微分方程**，解出 $f$ 的形式。
>
> **这题的真正考点是"方法链"，请务必记住这条链**：
> $$\boxed{f(x)=\int_0^xf'\ \Longrightarrow\ \text{Cauchy 积分不等式}\ \Longrightarrow\ \int_0^1F^2dx\le\frac12\int_0^1f^2dx\ \Longrightarrow\ \text{合并得带常数的不等式}}$$
> **等号条件的标准问法**：令 Cauchy 取等（两个被积函数成比例），得到一个**一阶微分方程**，解出 $f$ 即答案。

#### 武器三：化为二重积分 + 分区域论证

**适用**：证明 $\int f(x)\int g\ge$ 某量、或含"唯一零点"的结构。

**例 11（第17届解答六同型，14分）** 设 $f,g$ 在 $[a,b]$ 上连续可导，$f(a)=g(b)=0$，$f$ 在 $(a,b)$ 上有唯一零点 $c$，且 $f'(x)g(x)-f(x)g'(x)>0$。证明
$$\int_a^b f(x)\left(\int_a^x g(s)ds\right)dx>0$$

**证明框架**：

**第一步：记 $G(x)=\int_a^x g(s)ds$，则 $G'(x)=g(x)$，$G(a)=0$。**
由分部积分：
$$\int_a^b f(x)G(x)dx=\Big[fG\Big]_a^b-\int_a^b f'(x)G(x)dx=f(b)G(b)-\int_a^b f'G\,dx$$
（用到 $f(a)=0$、$G(a)=0$）

**第二步：把单重积分写成二重积分**，便于分区域比较：
$$\int_a^b f(x)G(x)dx=\int_a^b\!\!\int_a^x f(x)g(s)\,ds\,dx$$
同理 $\displaystyle\int_a^b f'(x)G(x)dx=\int_a^b\!\!\int_a^x f'(x)g(s)\,ds\,dx$。

两式相减得
$$2\int_a^bfG=\int_a^b\!\!\int_a^x\big[f(x)g(s)-f'(x)g(s)\big]\cdots$$

**第三步：把区域 $D=\{(x,s):a\le s\le x\le b\}$ 按零点 $c$ 分成三块**，在每块上利用 $f'(x)g(x)-f(x)g'(x)>0$ 推出 $\frac{f(x)}{g(x)}$ 严格单调，从而被积函数定号。

**第四步：三个区域上的积分分别定号**，求和得正。∎

> **妙招**：这类题的通用套路是
> **① 把 $\int f\cdot(\text{积分})$ 写成二重积分；② 用"零点"把区域分块；③ 在每块上用单调性定号。**
> 看到条件 $f'g-fg'>0$，立刻想到 **$\big(\frac fg\big)'=\frac{f'g-fg'}{g^2}>0$** —— 这是"商求导"的信号！

---

## 3.4 阶段三自测清单

- [ ] 例 1：幂级数收敛域（**三个端点都要写**）
- [ ] 例 3：递推乘积级数放缩到几何级数（三步法）
- [ ] 例 5'：换元化一阶线性
- [ ] 例 7：一阶线性公式（必须秒算）
- [ ] 例 8：二阶常系数三种情形
- [ ] 例 9、10、11：三种证明武器的框架
- [ ] 能背出 $p$-级数判敛、Cauchy 积分不等式、$\big(\frac fg\big)'$ 公式

### 常见失分点

| 失分点 | 正确做法 |
|---|---|
| 幂级数只写收敛区间不判端点 | 端点必须单独判定，答案写成闭/半开区间 |
| 缺项幂级数当成一般幂级数 | 把 $x^2$ 当整体，或令 $t=x^2$ |
| 分离变量时丢了特解 | 检查除以的因子是否为 0 |
| 一阶线性公式记错符号 | 写成 $y'+Py=Q$ **标准形式**再套 |
| 证不等式只会求导 | 想 Cauchy 积分不等式、二重积分、单调性 |
| 看到 $f'g-fg'$ 没反应 | 立刻想到 $\big(\frac fg\big)'$ |
| 证明题不写"由…定理" | 每步引用定理名（罗尔/拉格朗日/柯西/Taylor），阅卷按步给分 |

---

## 3.5 三届压轴题考点对照（第 6 题）

| 届次 | 压轴题类型 | 核心武器 |
|---|---|---|
| 第15届 | 递推数列型级数收敛并求和 | 反函数法 + 几何级数放缩 |
| 第16届 | 含取整函数的二重级数收敛性 | 分段估计 + 与 $p$-级数比较 + Leibniz |
| 第17届 | 含"唯一零点"条件的积分不等式 | 分部积分 + 化二重积分 + 分区域定号 |

**规律**：压轴题**永远不难在知识，难在技巧组合**。三届分别考了"数列 + 级数"、"二重级数"、"积分不等式"。
**复习策略**：把上面三种武器的框架练熟，考试时按框架往上套，不要从零想。

---

## 附：本系列文档

- `数学竞赛非数学A类_考点总结与复习计划.md` —— 总纲、考点统计、54 天计划
- `阶段一_极限与一元微积分.md`
- `阶段二_多元微积分与空间几何.md`
- `阶段三_级数与微分方程与证明技巧.md`（本文件）

标"第X届第Y题"的例题为真题原题，标"同型"的为按真题套路编排的练习题。
